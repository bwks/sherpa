# Speed up SSH key generation in debug mode.
[profile.dev.package.num-bigint-dig]
opt-level = 2

[workspace]
resolver = "3"
members = ["client", "konst", "container", "data", "libvirt", "server", "template", "topology", "util"]
