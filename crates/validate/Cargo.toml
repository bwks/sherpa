[package]
name = "validate"
version = "0.3.78"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
topology = { path = "../topology" }

# Errors
anyhow = { workspace = true }

# Logging
tracing = { workspace = true }
