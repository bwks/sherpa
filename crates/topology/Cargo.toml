[package]
name = "topology"
version = "0.3.78"
edition = "2024"

[dependencies]
shared = { path = "../shared" }

# Errors
anyhow = { workspace = true }

# Se/Deserialzation
serde = { workspace = true }
serde_derive = { workspace = true }
toml = { workspace = true }
toml_edit = { workspace = true }
